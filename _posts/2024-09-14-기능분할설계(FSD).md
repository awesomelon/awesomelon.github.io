---
title: "기능 분할 설계 (Feature-Sliced Design, FSD)"
date: 2024-09-14 16:26:00 +0900
categories: [ENGINEERING, FRONTEND]
tags: [fsd, frontend, architecture, 프론트엔드 아키텍처]
author: j-ho
img_path: /assets/img/for_post/
pin: false
description: 대규모 프론트엔드 애플리케이션을 구조화하기 위한 현대적인 아키텍처 방법론인 FSD를 소개합니다.
---

> **📌 KEYWORDS**  
> `FSD` `Frontend Architecture` `모듈화` `계층 구조` `확장성`
{: .prompt-info }

---

## 🚀 소개

기능 분할 설계(Feature-Sliced Design, FSD)는 대규모 프론트엔드 애플리케이션을 구조화하기 위한 현대적인 아키텍처 방법론입니다.

FSD는 애플리케이션을 기능 단위로 분할하고, 각 기능을 독립적으로 개발, 테스트, 유지보수할 수 있도록 하는 것을 목표로 합니다.

---

## 🎯 FSD의 핵심 원칙

FSD는 다음과 같은 핵심 원칙을 기반으로 합니다:

| 원칙 | 설명 |
|:---|:---|
| 🎨 **기능 중심 설계** | 애플리케이션을 비즈니스 기능 단위로 구성 |
| 📚 **계층화** | 코드를 여러 계층으로 구분하여 관심사 분리 |
| ➡️ **단방향 의존성** | 상위 계층은 하위 계층에만 의존 |
| 🔌 **명시적 공개 인터페이스** | 각 모듈은 명확한 공개 API를 통해 상호작용 |
| 🧩 **구성 가능성** | 작은 단위의 기능을 조합하여 더 큰 기능 구성 |

---

## 🏗️ FSD의 주요 구성 요소

![FSD 구조 개요](2024-09-14-image1.png)
_FSD의 전체 구조_

---

### 📊 레이어 (Layers)

레이어는 FSD의 가장 상위 수준의 구조입니다. 각 레이어는 특정한 책임을 가지며, 애플리케이션의 복잡성을 관리하는 데 도움을 줍니다.

![레이어 구조](2024-09-14-image3.jpg)
_FSD 레이어 계층 구조_

#### 표준 FSD 레이어 구조

| 레이어 | 역할 | 예시 |
|:---|:---|:---|
| **app** | 애플리케이션 초기화 및 글로벌 설정 | 라우팅, 전역 상태, 전역 스타일 |
| **processes** | 복잡한 비즈니스 프로세스 관리 (선택적) | 다단계 체크아웃, 온보딩 |
| **pages** | 라우팅 가능한 화면 정의 | 홈페이지, 상품 페이지 |
| **widgets** | 재사용 가능한 UI 블록 | 헤더, 사이드바, 상품 목록 |
| **features** | 사용자 시나리오와 비즈니스 로직 | 로그인, 장바구니 추가 |
| **entities** | 도메인 객체와 관련 로직 | 사용자, 상품 |
| **shared** | 공유 유틸리티 및 UI 키트 | 버튼, API 클라이언트, 설정 |

---

#### 1️⃣ app 레이어

애플리케이션의 진입점입니다.

- 전역 상태 관리, 라우팅 설정, 전역 스타일 정의
- 예: `src/app/index.tsx`, `src/app/store.ts`, `src/app/routes.ts`

---

#### 2️⃣ processes 레이어 (선택적)

여러 페이지나 기능에 걸친 복잡한 비즈니스 프로세스를 관리합니다.

> **사용 예시**  
> - 다단계 체크아웃 프로세스  
> - 사용자 온보딩 흐름  
> - 복잡한 마법사형 UI
{: .prompt-info }

---

#### 3️⃣ pages 레이어

각 라우트에 해당하는 페이지 컴포넌트를 정의합니다.

- 하위 위젯과 기능을 조합하여 전체 페이지 구성
- 예: `src/pages/HomePage.tsx`, `src/pages/ProfilePage.tsx`

---

#### 4️⃣ widgets 레이어

독립적인 UI 블록으로, 여러 기능을 조합할 수 있습니다.

- 페이지 레이아웃의 주요 부분을 구성
- 예: `src/widgets/Header`, `src/widgets/Sidebar`, `src/widgets/ProductList`

---

#### 5️⃣ features 레이어

사용자가 수행할 수 있는 작업 또는 시나리오입니다.

- UI 요소와 관련 비즈니스 로직을 포함
- 예: `src/features/auth/LoginForm`, `src/features/cart/AddToCartButton`

---

#### 6️⃣ entities 레이어

도메인 모델과 관련된 비즈니스 로직을 포함합니다.

- 도메인 객체의 상태와 행동 정의
- 예: `src/entities/user`, `src/entities/product`

---

#### 7️⃣ shared 레이어

프로젝트 전반에서 사용되는 공통 유틸리티 및 UI 컴포넌트입니다.

- 비즈니스 로직과 무관한 재사용 가능한 코드
- 예: `src/shared/ui/Button`, `src/shared/lib/api`, `src/shared/config`

---

![레이어 의존성](2024-09-14-image4.png)
_각 레이어는 하위 레이어에만 의존 가능_

> **⚠️ 중요**  
> 각 레이어는 하위 레이어에만 의존할 수 있어, 단방향 데이터 흐름을 보장합니다.
{: .prompt-warning }

---

### 🍕 슬라이스 (Slices)

슬라이스는 각 레이어 내에서 특정 기능 영역이나 도메인을 나타내는 하위 디렉토리입니다.

#### 슬라이스의 특징

- 특정 비즈니스 도메인이나 기능을 캡슐화
- 독립적으로 개발 및 테스트 가능
- 다른 슬라이스와의 의존성을 명시적으로 관리

#### 예시 구조
```text
src/
  features/
    auth/              // 인증 관련 슬라이스
    product-catalog/   // 상품 목록 관련 슬라이스
    shopping-cart/     // 장바구니 관련 슬라이스
  entities/
    user/             // 사용자 엔티티 슬라이스
    product/          // 상품 엔티티 슬라이스
```
{: .nolineno }

---

### 📦 세그먼트 (Segments)

세그먼트는 슬라이스 내부의 코드를 목적에 따라 더 세분화한 것입니다.

#### 주요 세그먼트

| 세그먼트 | 역할 |
|:---|:---|
| **ui** | UI 컴포넌트 |
| **model** | 비즈니스 로직 (상태 관리, 액션, 셀렉터) |
| **api** | 외부 서비스와의 통신 |
| **lib** | 유틸리티 함수 |
| **config** | 설정 및 상수 |

#### 예시 구조
```text
features/
  auth/
    ui/
      LoginForm.tsx
      RegisterForm.tsx
    model/
      authSlice.ts
      authSelectors.ts
    api/
      authApi.ts
    lib/
      passwordValidation.ts
    config/
      authConstants.ts
```
{: .nolineno }

---

### 🔌 Public API

Public API는 각 슬라이스나 세그먼트가 외부에 노출하는 인터페이스입니다.

#### 역할

- 내부 구현을 캡슐화
- 외부와의 상호작용 지점을 명확히 정의
- 모듈 간 의존성을 관리

#### 구현 방법

각 슬라이스나 세그먼트의 루트에 `index.ts` 파일을 생성하고, 외부에서 사용할 항목만 명시적으로 `export`합니다.
```typescript
// features/auth/index.ts

export { LoginForm } from './ui/LoginForm';
export { RegisterForm } from './ui/RegisterForm';
export { login, logout } from './model/authSlice';
export { selectUser } from './model/authSelectors';
export { fetchUser } from './api/authApi';
```
{: file='features/auth/index.ts' }

> **💡 캡슐화의 이점**  
> Public API를 통해 내부 구현을 숨기면, 리팩토링 시 외부에 영향을 주지 않고 내부 구조를 변경할 수 있습니다.
{: .prompt-tip }

---

## 🛠️ FSD 실제 적용

### 프로젝트 구조 예시
```text
src/
  app/
    index.tsx
    store.ts
    routes.ts
  processes/
    checkout/
  pages/
    HomePage/
    ProductPage/
    CartPage/
  widgets/
    Header/
    Footer/
    ProductList/
  features/
    auth/
    product-search/
    add-to-cart/
  entities/
    user/
    product/
  shared/
    ui/
    lib/
    config/
```
{: .nolineno }

---

### 컴포넌트 구성 예시

페이지는 위젯과 기능을 조합하여 구성합니다:
```typescript
// pages/HomePage/index.tsx
import { Header } from 'widgets/Header';
import { ProductList } from 'widgets/ProductList';
import { Footer } from 'widgets/Footer';
import { ProductSearch } from 'features/product-search';

export const HomePage = () => (
  <>
    <Header />
    <main>
      <ProductSearch />
      <ProductList />
    </main>
    <Footer />
  </>
);
```
{: file='pages/HomePage/index.tsx' }

---

### 상태 관리 통합

FSD는 특정 상태 관리 라이브러리에 종속되지 않지만, Redux, MobX 등과 잘 통합됩니다.

#### Redux 예시

**엔티티 슬라이스 정의:**
```typescript
// entities/product/model/productSlice.ts
import { createSlice } from '@reduxjs/toolkit';

const productSlice = createSlice({
  name: 'product',
  initialState: {
    items: [],
    loading: false,
  },
  reducers: {
    setProducts: (state, action) => {
      state.items = action.payload;
    },
    setLoading: (state, action) => {
      state.loading = action.payload;
    },
  },
});

export const { actions, reducer } = productSlice;
```
{: file='entities/product/model/productSlice.ts' }

**전역 스토어 설정:**
```typescript
// app/store.ts
import { configureStore } from '@reduxjs/toolkit';
import { reducer as productReducer } from 'entities/product';
import { reducer as userReducer } from 'entities/user';

export const store = configureStore({
  reducer: {
    product: productReducer,
    user: userReducer,
  },
});

export type RootState = ReturnType<typeof store.getState>;
export type AppDispatch = typeof store.dispatch;
```
{: file='app/store.ts' }

---

## ✅ FSD 장점

FSD를 적용하면 다음과 같은 이점을 얻을 수 있습니다:

| 장점 | 설명 |
|:---|:---|
| 🧩 **모듈성** | 기능 단위로 코드를 구성하여 재사용성과 유지보수성 향상 |
| 📈 **확장성** | 새로운 기능 추가나 기존 기능 수정이 용이 |
| 🧪 **테스트 용이성** | 각 슬라이스와 세그먼트를 독립적으로 테스트 가능 |
| 👥 **팀 협업** | 기능 단위로 작업을 분배하여 병렬 개발 가능 |
| 🗺️ **코드 네비게이션** | 일관된 구조로 코드 탐색이 쉬움 |
| 🔗 **의존성 관리** | 명시적인 의존성으로 코드의 예측 가능성 향상 |

> **🎯 핵심 가치**  
> FSD는 단순히 폴더 구조가 아닌, 코드의 품질과 유지보수성을 향상시키는 사고 방식입니다.
{: .prompt-tip }

---

## ⚠️ FSD 적용 시 고려 사항

FSD를 도입하기 전에 다음 사항을 고려해야 합니다:

### 1. 학습 곡선

팀 전체가 FSD 개념을 이해하고 동의해야 합니다.

> **📚 학습 리소스**  
> - [공식 문서](https://feature-sliced.design)  
> - [예제 코드(Todo App)](https://github.com/awesomelon/fsd-todo)
{: .prompt-info }

### 2. 오버엔지니어링 주의

작은 프로젝트에서는 복잡도가 증가할 수 있습니다.

> **⚖️ 적용 기준**  
> - 프로젝트 규모: 중대형 이상  
> - 팀 크기: 3명 이상  
> - 예상 수명: 장기 유지보수
{: .prompt-warning }

### 3. 일관성 유지

프로젝트 전반에 걸쳐 FSD 규칙을 일관되게 적용해야 합니다.

### 4. 리팩토링 비용

기존 프로젝트를 FSD로 전환하는 데 상당한 노력이 필요할 수 있습니다.

> **🔄 점진적 마이그레이션**  
> 전체를 한 번에 변경하기보다는, 새로운 기능부터 FSD를 적용하고 점진적으로 확대하는 것을 권장합니다.
{: .prompt-tip }

---

## 🎓 결론

FSD는 대규모 프론트엔드 프로젝트를 효과적으로 구조화하는 강력한 방법론입니다.

**기능 중심의 모듈화된 접근 방식**을 통해 코드의 재사용성, 유지보수성, 확장성을 크게 향상시킬 수 있습니다.

적절히 적용된다면, FSD는 복잡한 프론트엔드 애플리케이션 개발의 많은 일반적인 문제를 해결하고, 더 나은 코드 구조와 개발 경험을 제공할 것입니다.

---

## 📚 참고 자료

- [FSD 공식 문서](https://feature-sliced.design)
- [예제 코드 - Todo App](https://github.com/awesomelon/fsd-todo)
- [Feature-Sliced Design: The Best Frontend Architecture](https://dev.to/m_midas/feature-sliced-design-the-best-frontend-architecture-4noj)